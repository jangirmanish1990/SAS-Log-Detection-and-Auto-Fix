Run the pipeline in dry-run mode using the sample data files.
Execute: python main.py --log sample_data/sample.log --egp sample_data/sample_project.egp --dry-run
Show the full output and explain what each line of the run_log means.
