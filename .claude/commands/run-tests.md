Run the full pytest suite and report results.
Execute: pytest -v --tb=short
If any tests fail, show the failure details and suggest what might have caused them.
If all pass, confirm the count and which modules are covered.
