Show the EGP code node line offset map for the sample project.
Execute: python main.py --log sample_data/sample.log --egp sample_data/sample_project.egp --dry-run
Extract and display only the EGP line map section from the output.
Explain what the line ranges mean and how they relate to the SAS log line numbers.
