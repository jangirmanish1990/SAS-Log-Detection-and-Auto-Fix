Run the linter and formatter on the entire codebase.
Execute these commands in order:
1. black .
2. ruff check --fix .
Report any issues found. If black reformats files, list which ones changed.
If ruff finds violations it cannot auto-fix, explain each one.
